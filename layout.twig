<!doctype html>
<html lang="{{ page.seoLang }}">
	
		{% include "head.twig" %}
		
		{% include basekit.headScript %}
		
		<script type="text/javascript">
			$(function() {
				$('a[href*=#]:not([href=#])').click(function() {
					if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
						var target = $(this.hash);
						target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
						if (target.length) {
							$('html,body').animate({
								scrollTop: target.offset().top
							}, 1000);
							return false;
						}
					}
				});
			});
		</script>

	<body class="{{ page.backgroundClass }}">

<header class="bk-header">
	<div class="logo">
		{{widget('logo', 'logo', {"defaultlogo": asset('images/logo.png')})|raw}}
	</div>
	    <div class="companyheader wrapper clearfix">
	        <div class="header-left">
	            {{widget('content', 'footercolumn1content', {"content":"<h4>TELEPHONE: +1 512-291-8222</h4>"})|raw}}
	        </div>
	        <div class="header-center">
	            {{widget('companyname', 'companyname', {"business": "COFFEE LAB"})|raw}}
	        </div>
	        <div class="header-right">
	        	{{widget('content', 'footercolumn2content', {"content":"<h4>OPEN TIL 11PM EVERYDAY</h4>"})|raw}}
	        </div>
	    </div>       
</header>
		<div class="canvas">

		<nav class="nav">
			{{widget('extendednavigation', 'navigationheader', {})|raw}}
		</nav>

		<div class="page-wrap">
		
			{% include "header.twig" %}

			{% block content %}
			{% endblock %}

		</div>

		</div>
		
		<script type="text/javascript">
			!function() {
				function supportsSVG() {
					return !!document.createElementNS && !! document.createElementNS('http://www.w3.org/2000/svg', 'svg').createSVGRect
				}
				if (supportsSVG()) document.documentElement.className += ' svg'
			}()
		</script>

		{% include basekit.bodyScript %}

	</body>

	{% include "footer.twig" %}

</html>